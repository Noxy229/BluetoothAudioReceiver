<Window x:Class="BluetoothAudioReceiver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:BluetoothAudioReceiver.Services"
        Title="{Binding [AppTitle], Source={x:Static services:LocalizationService.Instance}}"
        Height="520" Width="400"
        MinHeight="450" MinWidth="360"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanMinimize"
        Background="Transparent">
    
    <Border Background="{StaticResource BackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <!-- Logo and Title -->
                    <StackPanel Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
                        <!-- Bluetooth Logo -->
                        <Border Width="26" Height="26" CornerRadius="6" Background="{StaticResource PrimaryBrush}">
                            <Viewbox Width="14" Height="14">
                                <Canvas Width="24" Height="24">
                                    <Path Fill="White" 
                                          Data="M17.71 7.71L12 2h-1v7.59L6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 11 14.41V22h1l5.71-5.71-4.3-4.29 4.3-4.29zM13 5.83l1.88 1.88L13 9.59V5.83zm1.88 10.46L13 18.17v-3.76l1.88 1.88z"/>
                                </Canvas>
                            </Viewbox>
                        </Border>
                        <TextBlock Text="{Binding [AppTitle], Source={x:Static services:LocalizationService.Instance}}"
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <!-- Window Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="─" 
                                Click="MinimizeButton_Click"
                                Style="{StaticResource TitleBarButtonStyle}"
                                FontSize="11"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                ToolTip="Minimieren"
                                AutomationProperties.Name="Minimieren"/>
                        <Button Content="✕" 
                                Click="CloseButton_Click"
                                Style="{StaticResource CloseButtonStyle}"
                                FontSize="11"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                ToolTip="Schließen"
                                AutomationProperties.Name="Schließen"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="20,16,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Status Header -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Status}" 
                                   FontSize="24" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding StatusDetail}" 
                                   FontSize="12" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- Action Buttons (top right) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Button x:Name="HelpButton"
                                Click="HelpButton_Click"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                ToolTip="{Binding [Help], Source={x:Static services:LocalizationService.Instance}}"
                                AutomationProperties.Name="{Binding [Help], Source={x:Static services:LocalizationService.Instance}}"
                                Padding="6"
                                Margin="0,0,4,0">
                            <TextBlock Text="❓" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                        <Button x:Name="SettingsButton"
                                Click="SettingsButton_Click"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                ToolTip="{Binding [Settings], Source={x:Static services:LocalizationService.Instance}}"
                                AutomationProperties.Name="{Binding [Settings], Source={x:Static services:LocalizationService.Instance}}"
                                Padding="6">
                            <Viewbox Width="16" Height="16">
                                <Canvas Width="24" Height="24">
                                    <Path Fill="{StaticResource TextSecondaryBrush}" 
                                          Data="M19.14,12.94c0.04-0.3,0.06-0.61,0.06-0.94c0-0.32-0.02-0.64-0.07-0.94l2.03-1.58c0.18-0.14,0.23-0.41,0.12-0.61 l-1.92-3.32c-0.12-0.22-0.37-0.29-0.59-0.22l-2.39,0.96c-0.5-0.38-1.03-0.7-1.62-0.94L14.4,2.81c-0.04-0.24-0.24-0.41-0.48-0.41 h-3.84c-0.24,0-0.43,0.17-0.47,0.41L9.25,5.35C8.66,5.59,8.12,5.92,7.63,6.29L5.24,5.33c-0.22-0.08-0.47,0-0.59,0.22L2.74,8.87 C2.62,9.08,2.66,9.34,2.86,9.48l2.03,1.58C4.84,11.36,4.8,11.69,4.8,12s0.02,0.64,0.07,0.94l-2.03,1.58 c-0.18,0.14-0.23,0.41-0.12,0.61l1.92,3.32c0.12,0.22,0.37,0.29,0.59,0.22l2.39-0.96c0.5,0.38,1.03,0.7,1.62,0.94l0.36,2.54 c0.05,0.24,0.24,0.41,0.48,0.41h3.84c0.24,0,0.44-0.17,0.47-0.41l0.36-2.54c0.59-0.24,1.13-0.56,1.62-0.94l2.39,0.96 c0.22,0.08,0.47,0,0.59-0.22l1.92-3.32c0.12-0.22,0.07-0.47-0.12-0.61L19.14,12.94z M12,15.6c-1.98,0-3.6-1.62-3.6-3.6 s1.62-3.6,3.6-3.6s3.6,1.62,3.6,3.6S13.98,15.6,12,15.6z"/>
                                </Canvas>
                            </Viewbox>
                        </Button>
                    </StackPanel>
                </Grid>
                
                <!-- Selected Device Card -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Bluetooth Icon -->
                        <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource SurfaceVariantBrush}">
                            <Viewbox Width="20" Height="20">
                                <Canvas Width="24" Height="24">
                                    <Path Fill="{StaticResource PrimaryBrush}" 
                                          Data="M17.71 7.71L12 2h-1v7.59L6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 11 14.41V22h1l5.71-5.71-4.3-4.29 4.3-4.29zM13 5.83l1.88 1.88L13 9.59V5.83zm1.88 10.46L13 18.17v-3.76l1.88 1.88z"/>
                                </Canvas>
                            </Viewbox>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                            <TextBlock Text="{Binding SelectedDevice.Name, FallbackValue='Kein Gerät ausgewählt'}" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="{Binding SelectedDevice.DisplayStatus, FallbackValue='Wähle ein Gerät aus der Liste'}" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Device List -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- List Header -->
                        <Grid Margin="4,0,4,10">
                            <TextBlock Text="{Binding [Devices], Source={x:Static services:LocalizationService.Instance}}" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                            <Button Content="⟳" 
                                    Command="{Binding RefreshDevicesCommand}"
                                    HorizontalAlignment="Right"
                                    FontSize="14"
                                    Padding="6,2"
                                    Background="Transparent"
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    ToolTip="{Binding [Refresh], Source={x:Static services:LocalizationService.Instance}}"
                                    AutomationProperties.Name="{Binding [Refresh], Source={x:Static services:LocalizationService.Instance}}"/>
                        </Grid>
                        
                        <!-- Device ListView -->
                        <ListBox Grid.Row="1" 
                                 ItemsSource="{Binding Devices}" 
                                 SelectedItem="{Binding SelectedDevice}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="border" 
                                                        Background="{StaticResource SurfaceVariantBrush}"
                                                        BorderBrush="Transparent"
                                                        BorderThickness="2"
                                                        CornerRadius="8"
                                                        Padding="12,10"
                                                        Margin="0,2"
                                                        Cursor="Hand">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <!-- Music Icon -->
                                                        <Border Width="32" Height="32" CornerRadius="6" 
                                                                Background="{StaticResource SurfaceBrush}">
                                                            <Viewbox Width="14" Height="14">
                                                                <Canvas Width="24" Height="24">
                                                                    <Path Fill="{StaticResource TextSecondaryBrush}" 
                                                                          Data="M12 3v10.55c-.59-.34-1.27-.55-2-.55-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4V7h4V3h-6z"/>
                                                                </Canvas>
                                                            </Viewbox>
                                                        </Border>
                                                        
                                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                                            <TextBlock Text="{Binding Name}" 
                                                                       FontSize="13" 
                                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                                            <TextBlock Text="{Binding DisplayStatus}" 
                                                                       FontSize="10" 
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       Margin="0,1,0,0"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="border" Property="Background" Value="#0D0078D4"/>
                                                    </Trigger>
                                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsSelected" Value="True"/>
                                                            <Condition Property="IsKeyboardFocused" Value="True"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextPrimaryBrush}"/>
                                                    </MultiTrigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsMouseOver" Value="True"/>
                                                            <Condition Property="IsSelected" Value="False"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                                                    </MultiTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                        
                        <!-- Empty State -->
                        <TextBlock Grid.Row="1"
                                   Text="{Binding [NoDevicesFound], Source={x:Static services:LocalizationService.Instance}}"
                                   TextAlignment="Center"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   LineHeight="18">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Devices.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
                
                <!-- Error Message -->
                <TextBlock Grid.Row="3"
                           Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorBrush}"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <!-- Action Buttons -->
                <Grid Grid.Row="4" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Content="{Binding [Connect], Source={x:Static services:LocalizationService.Instance}}"
                            Command="{Binding OpenConnectionCommand}"
                            Style="{StaticResource PrimaryButtonStyle}">
                        <Button.IsEnabled>
                            <MultiBinding>
                                <MultiBinding.Converter>
                                    <local:CanConnectConverter xmlns:local="clr-namespace:BluetoothAudioReceiver"/>
                                </MultiBinding.Converter>
                                <Binding Path="SelectedDevice"/>
                                <Binding Path="IsConnected"/>
                                <Binding Path="IsConnecting"/>
                            </MultiBinding>
                        </Button.IsEnabled>
                    </Button>
                    
                    <Button Grid.Column="2"
                            Content="{Binding [Disconnect], Source={x:Static services:LocalizationService.Instance}}"
                            Command="{Binding CloseConnectionCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            IsEnabled="{Binding IsConnected}"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
